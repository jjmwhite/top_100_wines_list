# README

### To set up, navigate to root directory and run: 
* ```bundle install```
* ```npm install```

### To run locally: 
* ```bundle exec rails s```
* ```run run watch```
* Navigate to http://localhost:3000